disallowed-methods = [
    { path = "std::process::Command::new", reason = "the UI redraws constantly, and on Windows each console program a GUI app starts flashes a window and costs tens of milliseconds; use an OS API instead" },
]
